# =====================================================
# SUPABASE CONFIGURATION (REQUIRED FOR PRODUCTION)
# =====================================================
# Get these from: https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Service role key for admin operations (server-side only)
# WARNING: Keep this secret! Never expose in client-side code
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# =====================================================
# SITE CONFIGURATION
# =====================================================
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Admin emails (comma-separated)
NEXT_PUBLIC_ADMIN_EMAILS=admin@medjob.ie,christopher.faherty@gmail.com

# =====================================================
# OPTIONAL: ANTHROPIC API (PDF PARSING)
# =====================================================
ANTHROPIC_API_KEY=sk-ant-your-key-here

# =====================================================
# OPTIONAL: EMAIL NOTIFICATIONS
# =====================================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM_EMAIL=noreply@medjob.ie
SMTP_FROM_NAME=MedJob IE

# =====================================================
# OPTIONAL: WEB SCRAPING
# =====================================================
# Path for Playwright browser binaries (if using Playwright)
PLAYWRIGHT_BROWSERS_PATH=/path/to/browsers

# =====================================================
# DEVELOPMENT SETTINGS
# =====================================================
# Set to 'true' to force localStorage fallback (development only)
# Useful for testing without Supabase or for offline development
NEXT_PUBLIC_USE_LOCAL_STORAGE_FALLBACK=false

# Enable debug logging
DEBUG=false

# =====================================================
# INSTRUCTIONS
# =====================================================
# 1. Copy this file to .env.local:
#    cp .env.example .env.local
#
# 2. Create a Supabase project at https://app.supabase.com
#
# 3. Run the database schema:
#    - Go to SQL Editor in Supabase dashboard
#    - Copy content from supabase/migrations/20260208_initial_schema.sql
#    - Execute the SQL
#
# 4. Get your API keys:
#    - Navigate to Project Settings > API
#    - Copy URL to NEXT_PUBLIC_SUPABASE_URL
#    - Copy anon/public key to NEXT_PUBLIC_SUPABASE_ANON_KEY
#    - Copy service_role key to SUPABASE_SERVICE_ROLE_KEY
#
# 5. Configure authentication:
#    - Go to Authentication > Providers
#    - Enable Email provider
#    - Configure email templates
#
# 6. Start development server:
#    npm run dev
#
# For detailed setup instructions, see SUPABASE_SETUP.md
